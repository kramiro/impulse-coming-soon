const field=document.getElementById("bubbles");const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;const sizes=[38,52,68,84,108,136,164];function addBubble(){const b=document.createElement("button");b.className="bubble";b.type="button";const s=sizes[Math.floor(Math.random()*sizes.length)];b.style.setProperty("--size",`${s}px`);b.style.setProperty("--x",`${4+Math.random()*90}vw`);b.style.setProperty("--drift",`${-50+Math.random()*100}px`);b.style.setProperty("--duration",`${13+Math.random()*11}s`);b.style.setProperty("--delay",`${-Math.random()*20}s`);b.addEventListener("click",()=>{if(b.classList.contains("pop"))return;b.classList.add("pop");setTimeout(()=>{b.remove();field.append(addBubble())},420)});return b}for(let i=0;i<(reduced?8:15);i++)field.append(addBubble());
